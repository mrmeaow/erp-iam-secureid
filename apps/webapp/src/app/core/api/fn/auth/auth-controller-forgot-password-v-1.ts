/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseDto } from '../../models/api-response-dto';
import { ForgotPasswordDto } from '../../models/forgot-password-dto';
import { SuccessResponseDto } from '../../models/success-response-dto';

export interface AuthControllerForgotPasswordV1$Params {
      body: ForgotPasswordDto
}

export function authControllerForgotPasswordV1(http: HttpClient, rootUrl: string, params: AuthControllerForgotPasswordV1$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseDto & {
'data'?: SuccessResponseDto;
}>> {
  const rb = new RequestBuilder(rootUrl, authControllerForgotPasswordV1.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseDto & {
      'data'?: SuccessResponseDto;
      }>;
    })
  );
}

authControllerForgotPasswordV1.PATH = '/v1/auth/forgot-password';
