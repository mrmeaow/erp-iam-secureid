/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseDto } from '../../models/api-response-dto';
import { AuthTokensDto } from '../../models/auth-tokens-dto';
import { RefreshDto } from '../../models/refresh-dto';

export interface AuthControllerRefreshV1$Params {
      body: RefreshDto
}

export function authControllerRefreshV1(http: HttpClient, rootUrl: string, params: AuthControllerRefreshV1$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseDto & {
'data'?: AuthTokensDto;
}>> {
  const rb = new RequestBuilder(rootUrl, authControllerRefreshV1.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseDto & {
      'data'?: AuthTokensDto;
      }>;
    })
  );
}

authControllerRefreshV1.PATH = '/v1/auth/refresh';
