# Internal use only

### Generate new key-pairs

> Using `RS256` algorithm key-pairs for JWT signing and verification

```bash
pnpm meaow keys
```

**Script**: `infra/commands/keys.ts`

**Output**: `keys/private.pem` and `keys/public.pem`